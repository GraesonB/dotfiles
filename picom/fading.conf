fading = true;
fade-delta = 16;              # Lower = smoother, slower fade (1–30 is typical)
fade-in-step = 0.07;          # How much opacity increases per frame
fade-out-step = 0.07;         # How much opacity decreases per frame
